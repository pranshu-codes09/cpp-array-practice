#include <iostream>
using namespace std;

int main() {
    
    int arr[] = {2,3,2,4};
    int size= sizeof(arr)/sizeof(int);
    int product = 1;
    
    for(int i = 0;i<size;i++){
        product = product * arr[i];
     }
     cout << "Product is: "<<product;
    return 0;
}